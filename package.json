{
  "name": "flex-promise-chain",
  "version": "0.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "node .",
    "dev": "nodemon --exec npm start",
    "deploy": "npm version --no-git-tag-version patch && kinvey flex deploy",
    "status": "kinvey flex job && kinvey flex status"
  },
  "devDependencies": {
    "nodemon": "^1.18.7"
  },
  "dependencies": {
    "kinvey-flex-sdk": "^3.2.0",
    "request": "^2.88.0",
    "request-promise": "^4.2.2"
  }
}
